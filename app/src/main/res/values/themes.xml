<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use AppCompat theme to support AppCompatActivity features like per-app language -->
    <style name="Theme.RadianceApp" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#010101</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.RadianceApp</item>
    </style>
</resources>
